<!-- <div class="modal fade" id="modal-add">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title">Borrow Books</h4>
        </div>
        <div class="modal-body">
            <form class="form-horizontal" method="post" id="form-books" action="/borrow_books/addBorrows">
              <div class="box-body">

                <div class="form-group">
                    <label class="col-sm-2 control-label" >Student Name</label>
                    <div class="col-sm-10">
                        <select class="form-control" name="user_id">
                            <option>Select Students</option>
                        <%for(const row of students) {%>
                            <option value="<%=row.id%>" style="font-weight: bold;"><%=row.firstname%> <%=row.lastname%></option>
                        <%}%>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label class="col-sm-2 control-label" >Book Name</label>
                    <div class="col-sm-10">
                        <select class="form-control" name="book_id">
                            <option>Select Books</option>
                        <%for(const row of books) {%>
                            <option value="<%=row.id%>" style="font-weight: bold;"><%=row.name%></option>
                        <%}%>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label class="col-sm-2 control-label" >Book Category</label>
                    <div class="col-sm-10">
                        <select class="form-control" name="category_id">
                            <option>Select Category</option>
                        <%for(const row of category) {%>
                            <option value="<%=row.id%>" style="font-weight: bold;"><%=row.name%></option>
                        <%}%>
                        </select>
                    </div>
                </div>

                <div class="modal-footer">
                  <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
                  <button type="submit" name="btn_submit" id="btn-save" class="btn btn-primary">Save Books</button>
                </div>

              </div>
            </form>
        </div>
       
      </div>
      <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
  </div> -->